const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('registro') === 'ok') {
          alert("✅ Usuario registrado correctamente");
          // También puedes usar un div en vez del alert
		  }